export interface CacheSchema {
    databases: string[];
    collections: {
        [database: string]: string[];
    };
}
export declare class Cache {
    private schema;
    constructor();
    listDatabases(): string[];
    listCollections(database: string): string[];
    cacheDatabases(databases: string[]): void;
    cacheCollections(database: string, collections: string[]): void;
    refreshCache(): void;
}
