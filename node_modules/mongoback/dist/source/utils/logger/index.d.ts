import { LogOptions } from '../../interfaces/options';
import { ExportSchema } from '../../interfaces/result';
export declare class Logger {
    private base;
    private command;
    private mongoexport;
    private expectedCollections;
    private actualCollections;
    private spinners;
    constructor(options: LogOptions);
    exportingDatabase(db: string): void;
    exportingCollectionStart(db: string, collection: string): void;
    exportingCollectionStop(db: string, collection: string, succeded: boolean): void;
    printCommand(cmd: string): void;
    printMongoexport(log: string, success: boolean): void;
    printExpectedCollections(expected: ExportSchema): void;
    printExportedCollections(actual: ExportSchema): void;
    warn(message: string, error: any): void;
}
