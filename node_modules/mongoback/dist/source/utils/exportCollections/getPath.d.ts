import { ExportingCollection } from '../../interfaces/result';
import { Options } from '../../interfaces/options';
export declare function getPath(db: string, collection: ExportingCollection, options: Options): string;
