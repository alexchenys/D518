import { ExportingCollection } from '../../interfaces/result';
import { ConnectionOptions } from '../../interfaces/options';
export declare function getCommand(database: string, parsedCollection: ExportingCollection, options: ConnectionOptions, outPath: string): string;
