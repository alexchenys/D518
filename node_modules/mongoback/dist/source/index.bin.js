#!/usr/bin/env node
"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var yargs_1 = __importDefault(require("yargs"));
var bin_1 = require("./utils/bin");
yargs_1.default
    .scriptName('mongoback')
    .command('export', 'Export your database with a powered mongoexport', function () {
    return {};
}, function (argv) { return __awaiter(void 0, void 0, void 0, function () {
    var args, options, askUri, askCollections, askDestination;
    return __generator(this, function (_a) {
        switch (_a.label) {
            case 0:
                args = argv;
                options = {
                    uri: args.uri,
                    host: args.host,
                    port: args.port,
                    username: args.username,
                    password: args.password,
                    authenticationDatabase: args.authDatabase,
                    authenticationMechanism: args.authMechanism,
                    srv: args.srv,
                    replicaSetName: args.replicaSetName,
                    ssl: args.ssl,
                    sslCAFile: args.sslCAFile,
                    sslPEMKeyFile: args.sslPEMKeyFile,
                    sslPEMKeyPassword: args.sslPEMKeyPassword,
                    sslCRLFile: args.sslCRLFile,
                    sslFIPSMode: args.sslFIPSMode,
                    sslAllowInvalidCertificates: args.sslAllowInvalidCertificates,
                    sslAllowInvalidHostnames: args.sslAllowInvalidHostnames,
                    gssapiServiceName: args.gssapiServiceName,
                    gssapiHostName: args.gssapiHostName,
                    slaveOk: args.slaveOk,
                    readPreference: args.readPreference,
                    journal: args.journal,
                    ipv6: args.ipv6,
                    dbpath: args.dbpath,
                    directoryperdb: args.directoryperdb,
                    all: args.all,
                    databases: args.databases,
                    collections: args.collections,
                    systemCollections: args.systemCollections,
                    throwIfLackOfPermissions: args.throwIfLackOfPermissions,
                    warnIfLackOfPermissions: args.warnIfLackOfPermissions,
                    throwIfOneFails: args.throwIfOneFails,
                    warnIfOneFails: args.warnIfOneFails,
                    quiet: args.quiet,
                    verbose: args.verbose,
                    type: args.type,
                    jsonFormat: args.jsonFormat,
                    jsonArray: args.jsonArray,
                    pretty: args.pretty,
                    query: args.query,
                    fields: args.fields,
                    fieldFile: args.fieldFile,
                    noHeaderLine: args.noHeaderLine,
                    skip: args.skip,
                    limit: args.limit,
                    sort: args.sort,
                    forceTableScan: args.forceTableScan,
                    prependDbName: args.prependDbName,
                    fileName: args.fileName,
                    filePath: args.filePath,
                    absolutePath: args.absolutePath,
                    silent: args.silent,
                    log: args.log,
                    outDir: args.outDir,
                    outType: args.outType
                };
                askUri = args.askUri;
                askCollections = args.askCollections;
                askDestination = args.askDestination;
                return [4 /*yield*/, bin_1.mongoExportCli(options, { askUri: askUri, askCollections: askCollections, askDestination: askDestination })];
            case 1:
                _a.sent();
                return [2 /*return*/];
        }
    });
}); })
    .demandCommand(1, 'You must prompt the export command')
    .options({
    // Cli options
    'askUri': {
        alias: 'u',
        default: undefined,
        defaultDescription: 'By default it is asked if the option --uri is not specified',
        describe: 'If the connection uri will be asked to the user. If false, the uri deducted by the other options will be used.',
        type: 'boolean',
        group: 'Cli options'
    },
    'askCollections': {
        alias: 'c',
        default: true,
        describe: 'If the collections to be exported will be asked to the user. If false, the ones deducted by the other options will be used.',
        type: 'boolean',
        group: 'Cli options'
    },
    'askDestination': {
        alias: 'd',
        default: true,
        describe: 'If the outDir will be asked to the user. If false, the one deducted by the other options will be used.',
        type: 'boolean',
        group: 'Cli options'
    },
    // Main connection options
    'uri': {
        default: undefined,
        describe: 'The uri of the MongoDB connection. If it is specified, the other main connection options are ignored and (if not explicitly specified) the askUri will be set to false',
        type: 'string',
        group: 'Main connection options'
    },
    'host': {
        default: 'localhost',
        describe: 'The host of the MongoDB connection.',
        type: 'string',
        group: 'Main connection options'
    },
    'port': {
        default: 27017,
        describe: 'The port of the MongoDB connection.',
        type: 'number',
        group: 'Main connection options'
    },
    'username': {
        default: undefined,
        describe: 'The username of the MongoDB connection.',
        type: 'string',
        group: 'Main connection options'
    },
    'password': {
        default: undefined,
        describe: 'The password of the MongoDB connection.',
        type: 'string',
        group: 'Main connection options'
    },
    'authDatabase': {
        default: undefined,
        describe: 'The authentication database of the MongoDB connection.',
        type: 'string',
        group: 'Main connection options'
    },
    'authMechanism': {
        default: undefined,
        describe: 'The authentication mechanism of the MongoDB connection.',
        type: 'string',
        group: 'Main connection options'
    },
    'srv': {
        default: false,
        describe: 'If the MongoDB connection uri is an srv.',
        type: 'string',
        group: 'Main connection options'
    },
    // Advanced connection options
    'replicaSetName': {
        default: undefined,
        describe: 'The replicaSetName of the MongoDB connection.',
        type: 'string',
        group: 'Advanced connection options'
    },
    'ssl': {
        default: false,
        describe: 'If the MongoDB connection uses ssl or tls.',
        type: 'boolean',
        group: 'Advanced connection options'
    },
    'sslCAFile': {
        default: undefined,
        describe: 'Specifies the .pem file that contains both the TLS/SSL certificate and key.',
        type: 'string',
        group: 'Advanced connection options'
    },
    'sslPEMKeyFile': {
        default: undefined,
        describe: 'Specify the file name of the .pem file using relative or absolute paths.',
        type: 'string',
        group: 'Advanced connection options'
    },
    'sslPEMKeyPassword': {
        default: undefined,
        describe: 'Specifies the password to de-crypt the certificate-key file (i.e. --sslPEMKeyFile).',
        type: 'string',
        group: 'Advanced connection options'
    },
    'sslCRLFile': {
        default: undefined,
        describe: 'Specifies the .pem file that contains the Certificate Revocation List.',
        type: 'string',
        group: 'Advanced connection options'
    },
    'sslFIPSMode': {
        default: false,
        describe: 'Directs the mongoexport to use the FIPS mode of the installed OpenSSL library. Note: deprecated option of mongoexport.',
        type: 'boolean',
        group: 'Advanced connection options'
    },
    'sslAllowInvalidCertificates': {
        default: false,
        describe: 'Bypasses the validation checks for server certificates and allows the  use of invalid certificates.',
        type: 'boolean',
        group: 'Advanced connection options'
    },
    'sslAllowInvalidHostnames': {
        default: false,
        describe: 'Disables the validation of the hostnames in TLS/SSL certificates.',
        type: 'boolean',
        group: 'Advanced connection options'
    },
    'gssapiServiceName': {
        default: undefined,
        describe: 'Specify the hostname of a service using GSSAPI/Kerberos. Only required if the hostname of a machine does not match the hostname resolved by DNS.',
        type: 'string',
        group: 'Advanced connection options'
    },
    'gssapiHostName': {
        default: undefined,
        describe: 'Specify the hostname of a service using GSSAPI/Kerberos. Only required  if the hostname of a machine does not match the hostname resolved by DNS.',
        type: 'string',
        group: 'Advanced connection options'
    },
    'slaveOk': {
        default: false,
        describe: 'Sets the Read Preference to nearest, allowing mongoexport to read data from secondary replica set members. Note: Deprecated option of mongoexport.',
        type: 'boolean',
        group: 'Advanced connection options'
    },
    'readPreference': {
        default: undefined,
        describe: 'Specify the read preference for mongoexport. It can be a string such as \'primary\' or \'secondary\' or an object (config file only). If you want to pass the json object as a string, you must manually include it in apixes.',
        type: 'string',
        group: 'Advanced connection options'
    },
    'journal': {
        default: false,
        describe: 'Allows mongoexport operations to access the durability journal to ensure that the export is in a valid state. Note: Deprecated option of mongoexport.',
        type: 'boolean',
        group: 'Advanced connection options'
    },
    'ipv6': {
        default: false,
        describe: 'Enables IPv6 support that allows mongoexport to connect to the MongoDB instance using an IPv6 network. Note: Deprecated on mongoexport.',
        type: 'boolean',
        group: 'Advanced connection options'
    },
    'dbpath': {
        default: undefined,
        describe: 'Specifies the directory of the MongoDB data files. Note: Deprecated on mongoexport',
        type: 'string',
        group: 'Advanced connection options'
    },
    'directoryperdb': {
        default: false,
        describe: 'Use the --directoryperdb in conjunction with the corresponding option mongod, which allows mongoexport to export data from MongoDB that have every database’s files saved in discrete directories on disk. Note: Deprecated on mongoexport',
        type: 'boolean',
        group: 'Advanced connection options'
    },
    // Exported options
    'all': {
        default: false,
        describe: 'If all the collections of every database will be exported.',
        type: 'boolean',
        group: 'Exported options'
    },
    'databases': {
        default: undefined,
        describe: 'The databases that will be exported. All the collections of a database will be exported. Eventual exporting options passed to this option will overwrite the default ones. Note: on the cli, only strings are allowed.',
        type: 'string',
        group: 'Exported options'
    },
    'collections': {
        default: [],
        describe: 'The collections that will be exported. Eventual exporting options passed to this option will overwrite the default ones and the ones in the "database" option. Note: on the cli, only strings are allowed.',
        type: 'array',
        group: 'Exported options'
    },
    'systemCollections': {
        default: false,
        describe: 'If also system collections will be exported.',
        type: 'boolean',
        group: 'Exported options'
    },
    'throwIfLackOfPermissions': {
        default: false,
        describe: 'If for permissions causes there is an error while listing databases or collections of the MongoDB, an error will be thrown. If the value is false, the databases and collections that cannot be listed will be ignored and not be exported. Note: Actually all the errors that happen while listing databases or collections, not only the permission ones, will be thrown.',
        type: 'boolean',
        group: 'Exported options'
    },
    'warnIfLackOfPermissions': {
        default: false,
        describe: 'If for permissions causes there is an error while listing databases or collections of the MongoDB, a warning message will be logged. NB: Actually all the errors that happen while listing databases or collections, not only the permission ones, will be warned.',
        type: 'boolean',
        group: 'Exported options'
    },
    'throwIfOneFails': {
        default: false,
        describe: 'If the mongoexport of a collection fails, an error will be thrown. If the  value is false, the result of the function will have code PARTIAL(= 1), specifying that not all the expected collections were exported.',
        type: 'boolean',
        group: 'Exported options'
    },
    'warnIfOneFails': {
        default: false,
        describe: 'If the mongoexport of a collection fails, a warning will be logged.',
        type: 'boolean',
        group: 'Exported options'
    },
    // Exporting options
    'quiet': {
        default: false,
        describe: 'Runs mongoexport in a quiet mode that attempts to limit the amount of output.',
        type: 'boolean',
        group: 'Exporting options'
    },
    'verbose': {
        default: undefined,
        describe: 'Increases the amount of internal reporting returned on standard output or in log files. Increase the verbosity with the -v form by including the option  multiple times, (e.g. -vvvvv.). Value from 1 to 5',
        type: 'number',
        group: 'Exporting options'
    },
    'type': {
        default: undefined,
        choices: ['json', 'csv'],
        describe: 'Specifies the file type to export. Specify csv for CSV format or json for JSON format. If you specify csv, then you must also use either the --fields or the  --fieldFile option to declare the fields to export from the collection.',
        type: 'string',
        group: 'Exporting options'
    },
    'jsonFormat': {
        default: undefined,
        choices: ['canonical', 'relaxed'],
        describe: 'Modifies the output to use either canonical or relaxed mode of the MongoDB Extended JSON (v2) format.',
        type: 'string',
        group: 'Exporting options'
    },
    'jsonArray': {
        default: false,
        describe: 'Modifies the output of mongoexport to write the entire contents of the export  as a single JSON array. By default mongoexport writes data using one JSON document for every MongoDB document.',
        type: 'boolean',
        group: 'Exporting options'
    },
    'pretty': {
        default: false,
        describe: 'Outputs documents in a pretty-printed format JSON.',
        type: 'boolean',
        group: 'Exporting options'
    },
    'query': {
        default: undefined,
        describe: 'Provides a query as a JSON document (enclosed in quotes) to return matching documents in the export. You must enclose the query document in single  quotes (\'{ ... }\') to ensure that it does not interact with your shell environment. Starting in MongoDB 4.2, the query must be in  Extended JSON v2 format (either relaxed or canonical/strict mode), including enclosing the field names and operators in quotes. You can pass the argument either as a string (it will automatically be included in apixes) or as an object (only from config file).',
        type: 'string',
        group: 'Exporting options'
    },
    'fields': {
        default: [],
        describe: 'Specifies a field or fields to include in the export.',
        type: 'array',
        coerce: function (value) { return value && value.length ? value : undefined; },
        group: 'Exporting options'
    },
    'fieldFile': {
        default: undefined,
        describe: 'An alternative to --fields. The --fieldFile option allows you to specify  in a file the field or fields to include in the export and is only valid  with the --type option with value csv. The file must have only one field per line, and the line(s) must end with the LF character (0x0A).',
        type: 'string',
        group: 'Exporting options'
    },
    'noHeaderLine': {
        default: false,
        describe: 'By default, mongoexport includes the exported field names as the first line in a CSV output.',
        type: 'boolean',
        group: 'Exporting options'
    },
    'skip': {
        default: undefined,
        describe: 'Use --skip to control where mongoexport begins exporting documents.',
        type: 'number',
        group: 'Exporting options'
    },
    'limit': {
        default: undefined,
        describe: 'Specifies a maximum number of documents to include in the export.',
        type: 'number',
        group: 'Exporting options'
    },
    'sort': {
        default: undefined,
        describe: 'You can pass the argument either as a string (it will automatically be included in apixes) or as an object (only config file).',
        type: 'string',
        group: 'Exporting options'
    },
    'forceTableScan': {
        default: false,
        describe: 'Forces mongoexport to scan the data store directly instead of traversing the _id field index.',
        type: 'boolean',
        group: 'Exporting options'
    },
    'prependDbName': {
        default: undefined,
        describe: 'If the file name will be prepended by the database of the collection. The format is: "database_filename.extension".',
        type: 'boolean',
        group: 'Exporting options'
    },
    'fileName': {
        default: undefined,
        describe: 'A string which is the name of the file of the exported collection.',
        type: 'string',
        group: 'Exporting options'
    },
    'filePath': {
        default: undefined,
        describe: 'A string which is the path of the file of the exported collection.',
        type: 'string',
        group: 'Exporting options'
    },
    'absolutePath': {
        default: false,
        describe: 'If the filePath value is absolute and not relative to the outDir option.',
        type: 'boolean',
        group: 'Exporting options'
    },
    // Log options
    'silent': {
        default: false,
        describe: 'If nothing will be logged.',
        type: 'boolean',
        group: 'Log options'
    },
    'log': {
        default: ['base'],
        choices: ['base', 'command', 'mongoexport', 'expectedCollections', 'actualCollections'],
        describe: 'The log modes. If there is more than a mode, they must be specified in an array.',
        type: 'array',
        group: 'Log options'
    },
    // Out options
    'outDir': {
        default: './exported',
        describe: 'The path were the exported collections will be saved.',
        type: 'string',
        group: 'Out options'
    },
    'outType': {
        default: 'deep',
        choices: ['deep', 'flat'],
        describe: 'The type of the saving location.',
        type: 'string',
        group: 'Out options'
    },
    // Config file
    'options': {
        alias: 'o',
        describe: 'A path to a json config file. If an option is both on the file and in the command, the command one will be considered',
        config: true,
        configParser: function (path) { return require(path); }
    }
})
    .epilogue('For more information, find our manual at https://github.com/euberdeveloper/mongoback#readme')
    .argv;
//# sourceMappingURL=index.bin.js.map